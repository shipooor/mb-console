{
  "name": "magicblock-console",
  "version": "0.1.0",
  "private": true,
  "description": "Developer console for MagicBlock Ephemeral Rollups on Solana",
  "workspaces": [
    "packages/core",
    "packages/web",
    "packages/cli",
    "packages/mcp"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build -w packages/core && npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
